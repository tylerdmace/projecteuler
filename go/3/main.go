package main

import "fmt"

func main() {
	// number := 600851475143

	fmt.Printf("0: %v, %v: %v\r\n", numbers[0], len(numbers), numbers[len(numbers)-1])
}

func findPrimeFactors(n int) []int {
	sr := findSquareRoot(n)
}

func findSquareRoot(n int) int {
	for i := 1; i < n; i++ {
		if i*i > n {
			fmt.Printf("%v %v")
		}
	}
}
